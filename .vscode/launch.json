{
    "version": "0.2.1",
    "configurations": [
        {
            "name": "Run Lua",
            "type": "lua",
            "request": "launch",
            "program": "${file}",
            "noDebug": true
        }
    ]
}